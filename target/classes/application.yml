spring:
  application:
    name: api
  datasource:
    url: jdbc:mysql://localhost:3306/613801_shavlovskii1?autoReconnect=true&useSSL=false&characterEncoding=utf8&useUnicode=true
    username: root
    password: root
server:
  port: 3000

spring-hoc:
  cors:
    allow-credentials: true
    allowed-origins:
      - "https://my-frontend-application.com"
      - "https://jpomykala.com"
      - "http://localhost"
    allowed-methods:
      - GET
      - POST
      - PATCH
      - DELETE